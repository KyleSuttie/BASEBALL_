<br/>
  <% form_tag ( { :action => 'upload' }, :multipart => true) do%>
    <label for="file">File to Upload</label> <%= file_field_tag 'file', :accept => 'text/xml' %>
    <%= submit_tag ( "Upload" ) %>
  <% end %>

